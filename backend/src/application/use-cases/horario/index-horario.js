const cargarHorario = require('./cargar-horario');
const eliminarHorario = require('./eliminar-horario');
const verHorario = require('./ver-horario');

module.exports = {
  cargarHorario,
  eliminarHorario,
  verHorario,
};